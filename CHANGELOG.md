# 版本修改声明

## 2026-05-02

- 优化 DOCX 转换器：自动识别输入图片目录中的 `封面.*` 或 `cover.*`，并将其作为无图名封面插入到 `题目` 标题之前。
- 优化 DOCX 富文本输出：标题、正文与图注中的常见化学式和同位素表达会写入 Word 原生上下标格式，包括 `N2O`、`NH4+`、`NO2-`、`NO3-`、`O2`、`δ18O` 与 `m3` 等。
- 保持封面插入与正文图片相同的居中图片样式，同时不影响 Markdown 图片校验与 DOCX 图片数量校验。
- 修正忽略规则，使输入论文、生成输出和本地模板文件默认不进入版本控制。
